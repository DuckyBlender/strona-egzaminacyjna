console.log('Window size:', window.innerWidth, 'x', window.innerHeight);

window.addEventListener('resize', () => {
    console.log('Window size:', window.innerWidth, 'x', window.innerHeight);
    // window.open("https://google.com", "Google", "width=1000, height=500", )
});